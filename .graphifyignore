# The knowledge graph models active architecture, not immutable execution
# evidence. Current inputs remain here; historical receipts live in the archive.
/docs/structural_gates/receipts/

# V10/V11 controllers are archived. Keep the exclusion if an old experiment
# is temporarily restored; it must not dominate active code queries.
/scripts/lay-v10-*
/scripts/lay-v11-*
/scripts/lay_v10_*.rs.inc

# Graphify outputs and its local query/reflection memory are generated consumers
# of the source graph. Indexing them back into the corpus creates a recursive
# graph and can leak untracked service-only artifacts into tracked reports.
/graphify-out/

# Final execution facts are filled after acceptance; keep their binary hashes
# out of the architecture receipt embedded in those same accepted binaries.
/tech_debt/evidence/td121-release-1.0.72-execution-2026-09-14.md
